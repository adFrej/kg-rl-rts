[tool.poetry]
name = "kg-rl-rts"
version = "0.1.0"
description = ""
authors = ["adFrej <adfrej1@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
jupyter = "^1.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

